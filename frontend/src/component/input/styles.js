import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  input: {
    alignSelf: 'stretch',
    height: 40,
    margin: 16,
    borderWidth: 1,
    padding: 10,
  },
});

export default styles;
